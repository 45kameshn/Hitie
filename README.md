React + Vite app.

## Getting Started

```bash
npm install
npm run dev
```

Open the printed local URL (usually `http://localhost:5173`).

## Key Files

- `src/main.tsx` (bootstraps React)
- `src/App.tsx` (renders the page)
- `src/site/html.ts` (builds the HTML string used as `iframe.srcDoc`)

